#include<iostream>
#include<string
using namespace std;
bool isPal(string &str)
{
    int s=0;
    int e=str.length()-1;

    while(s<e)
    {
        if(str[s]!=str[e])
    }
}

int main()
{
    string str="mom";
    string str2=str;

    reverse(str.begin(), str.end())
}